# ─────────────────────────────────────────
#  MedBot — Full Requirements
#  Run: pip install -r requirements.txt
# ─────────────────────────────────────────

# Web Framework
flask==3.0.0
werkzeug==3.0.0

# HTTP
requests==2.31.0

# NLP & Transformers (BioBERT + SapBERT)
transformers==4.40.2
sentence-transformers==2.7.0

# LangGraph pipeline
langgraph>=0.1.0

# PyTorch — Lung model (EfficientNet B3)
torch==2.3.0
torchvision==0.18.0

# TensorFlow — Brain + Skin models
# ── Windows / Linux:
tensorflow==2.16.2
# ── Mac M1/M2/M3 → comment line above and run:
#    pip install tensorflow-macos tensorflow-metal

# Data & Image processing
numpy==1.26.4
pillow==10.3.0
h5py==3.11.0
scikit-learn==1.5.0
